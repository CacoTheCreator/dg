# Source files / trabajo previo
leadform.html
recursos/
CONTEXT_THE_DAILY_GRIND.md

# OS
.DS_Store

.claude/
node_modules
package-lock.json

# Wrangler local cache
.wrangler/

# Audit / render artifacts (regenerables)
og-templates/mobile-shots/
og-templates/mobile-shots-kraneo/

# Tooling local de render (no se sirve — ya excluido en .assetsignore)
scripts/

# Defensa: backups operacionales NUNCA viven dentro del repo
# (destino canónico: ~/dailygrind-backups, fuera del repo)
dailygrind-backups/
*-backups/
*.sql.gz
backup-dumps/
